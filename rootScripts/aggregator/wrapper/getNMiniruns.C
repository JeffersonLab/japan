void getNMiniruns(Int_t runNumber = 0) {
  Int_t miniruns = -1;
  // Get the post pan file, open it, find minirun, find its final entry, get that value, return
  // Open(runNumber); // FIXME For JAPAN version of code
  Printf("NMiniruns=%d",miniruns);
}
